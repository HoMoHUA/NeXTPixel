# راهنمای تنظیم نقش‌های کاربری

## 📋 نقش‌های سیستم

سیستم NextPixel از نقش‌های زیر پشتیبانی می‌کند:

1. **customer** - مشتری → پنل مشتری (`/panel/client/`)
2. **seller** - فروشنده → پنل فروش (`/panel/seller/`)
3. **designer** - طراح → پنل طراح (`/panel/designer/`)
4. **admin** - ادمین → پنل ادمین (`/panel/`)
5. **staff** - همکار عمومی → پنل اصلی (`/panel/`)

## 🔧 تنظیم نقش‌های کاربری در دیتابیس

### مرحله 1: افزودن ستون role

ابتدا فایل SQL زیر را در دیتابیس اجرا کنید:

```sql
USE `gcifyhuc_NP`;

-- افزودن ستون role
ALTER TABLE `users` 
ADD COLUMN IF NOT EXISTS `role` VARCHAR(50) DEFAULT NULL COMMENT 'نقش دقیق کاربر: seller, designer, admin, customer';

-- ایجاد ایندکس
ALTER TABLE `users` ADD INDEX IF NOT EXISTS `idx_role` (`role`);
```

یا از فایل `config/add-role-column.sql` استفاده کنید.

### مرحله 2: تنظیم نقش کاربران

برای تنظیم نقش هر کاربر، از دستورات SQL زیر استفاده کنید:

```sql
-- تنظیم نقش فروشنده
UPDATE `users` SET `role` = 'seller' WHERE `username` = 'نام_کاربری_فروشنده';

-- تنظیم نقش طراح
UPDATE `users` SET `role` = 'designer' WHERE `username` = 'نام_کاربری_طراح';

-- تنظیم نقش مشتری
UPDATE `users` SET `role` = 'customer' WHERE `username` = 'نام_کاربری_مشتری';

-- تنظیم نقش ادمین
UPDATE `users` SET `role` = 'admin' WHERE `username` = 'HoMo';
```

## 📝 نحوه کار سیستم لاگین

### 1. صفحه لاگین واحد

- فقط یک صفحه لاگین وجود دارد: `/login.php`
- کاربر می‌تواند بین "ورود همکاران" و "ورود مشتریان" انتخاب کند

### 2. فرآیند لاگین

1. کاربر نام کاربری و رمز عبور را وارد می‌کند
2. نوع کاربر (staff یا customer) را انتخاب می‌کند
3. سیستم بررسی می‌کند:
   - نام کاربری و رمز عبور درست است؟
   - نوع کاربر انتخاب شده با `user_type` در دیتابیس تطابق دارد؟
4. نقش دقیق کاربر از ستون `role` خوانده می‌شود
5. کاربر به پنل مناسب هدایت می‌شود

### 3. هدایت به پنل‌ها

بر اساس نقش کاربر (`role` در دیتابیس):

| نقش | مسیر هدایت |
|-----|------------|
| `customer` | `/panel/client/index.php` |
| `seller` | `/panel/seller/dashboard.php` |
| `designer` | `/panel/designer/index.php` |
| `admin` | `/panel/index.php` |
| `staff` | `/panel/index.php` |

## 🔐 نحوه تنظیم نقش توسط HoMo (مدیر اصلی)

مدیر اصلی (HoMo) می‌تواند نقش کاربران را از طریق دیتابیس تنظیم کند:

### مثال: ایجاد کاربر فروشنده

```sql
-- 1. ایجاد کاربر (یا استفاده از کاربر موجود)
INSERT INTO `users` (`username`, `email`, `password`, `display_name`, `user_type`, `status`, `role`) 
VALUES (
    'seller1', 
    'seller@nextpixel.ir', 
    '$2y$10$...',  -- پسورد هش شده
    'فروشنده نمونه',
    'staff',
    'active',
    'seller'
);

-- یا به‌روزرسانی نقش کاربر موجود
UPDATE `users` SET `role` = 'seller' WHERE `username` = 'seller1';
```

### مثال: ایجاد کاربر طراح

```sql
UPDATE `users` SET `role` = 'designer' WHERE `username` = 'designer1';
```

### مثال: ایجاد کاربر مشتری

```sql
INSERT INTO `users` (`username`, `email`, `password`, `display_name`, `user_type`, `status`, `role`) 
VALUES (
    'customer1', 
    'customer@example.com', 
    '$2y$10$...',  -- پسورد هش شده
    'مشتری نمونه',
    'customer',
    'active',
    'customer'
);
```

## ⚠️ نکات مهم

1. **ستون role اختیاری است**: اگر ستون `role` خالی باشد، از `user_type` استفاده می‌شود
2. **تنظیم نقش توسط HoMo**: فقط مدیر اصلی (HoMo) می‌تواند نقش کاربران را تنظیم کند
3. **لاگین واحد**: همه کاربران از `/login.php` وارد می‌شوند
4. **هدایت خودکار**: بعد از لاگین، کاربر به صورت خودکار به پنل مناسب هدایت می‌شود

## 🧪 تست

برای تست سیستم:

1. یک کاربر با نقش `seller` ایجاد کنید
2. از `/login.php` با انتخاب "ورود همکاران" وارد شوید
3. باید به `/panel/seller/dashboard.php` هدایت شوید

## 📁 فایل‌های مرتبط

- `/login.php` - صفحه لاگین اصلی
- `/auth.php` - پردازش لاگین و تعیین نقش
- `/panel/includes/auth.php` - میدلور احراز هویت
- `/config/add-role-column.sql` - اسکریپت SQL برای افزودن ستون role

